# Frontend (React + Vite)

Quick start:
- cd frontend
- npm ci
- npm run dev

Build:
- npm run build
- Serve dist with a static server or build Docker image (Dockerfile provided)

Notes:
- The frontend expects API at /api — in production set up a reverse proxy or CORS accordingly.
